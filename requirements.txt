# AI CC Reporting System v5
# requirements.txt
#
# FIX: "Client.__init__() got an unexpected keyword argument 'proxies'"
# Root cause: openai library meneruskan parameter 'proxies' ke httpx,
#             tapi httpx >= 0.28.0 sudah menghapus parameter tersebut.
# Solution  : pin httpx ke 0.27.2 (versi terakhir yang masih support 'proxies')

streamlit>=1.32.0
openai>=1.14.0,<2.0.0
httpx==0.27.2           # WAJIB — jangan upgrade ke 0.28.x
gspread>=6.0.0
google-auth>=2.28.0
Pillow>=10.0.0
pypdfium2==4.30.0
pandas>=2.0.0
